/* Gelistirme varsayilani. Paketleyici, Git disindaki yerel jetonu yalnizca
   uretilen WGT icindeki bu nesneye ekler. */
window.HM_PRIVATE = window.HM_PRIVATE || { tmdbToken: '', buildChannel: 'development' };
